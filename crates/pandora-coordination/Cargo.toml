[package]
name = "pandora-coordination"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
pandora-capability = { path = "../pandora-capability" }
pandora-fitness = { path = "../pandora-fitness" }
pandora-gene = { path = "../pandora-gene" }
pandora-registry = { path = "../pandora-registry" }
