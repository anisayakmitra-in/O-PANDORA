//! TEMPORARY legacy provider implementations.
//!
//! These modules are kept only for backward compatibility. They will
//! be removed once the plugin architecture lands. Enable with the
//! `legacy-ollama` cargo feature.

pub mod ollama;
