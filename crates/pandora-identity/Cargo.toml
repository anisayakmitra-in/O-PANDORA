[package]
name = "pandora-identity"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Pandora constitutional identity framework"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "1"
semver = "1"

[dev-dependencies]
serde_json = "1"
