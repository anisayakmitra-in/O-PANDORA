[package]
name = "pandora-registry"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

pandora-contract = { path = "../pandora-contract" }
