[package]
name = "pandora-vector-store"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

pandora-runtime = { path = "../pandora-runtime" }
