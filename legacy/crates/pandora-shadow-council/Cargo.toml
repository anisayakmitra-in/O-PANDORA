[package]
description = "Harness and gene registry — routing tasks to the right harness and gene"
keywords = ["ai", "runtime", "routing"]
publish = false
name = "pandora-shadow-council"
version.workspace = true
edition.workspace = true

[dependencies]
pandora-types = { path = "../pandora-types" }
serde.workspace = true
tracing.workspace = true
