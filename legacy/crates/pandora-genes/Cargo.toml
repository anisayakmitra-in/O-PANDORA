[package]
description = "21 built-in genes — shell, filesystem, git, http, docker, browser, code analysis"
keywords = ["ai", "runtime", "tools"]
publish = false
name = "pandora-genes"
version.workspace = true
edition.workspace = true

[dependencies]
pandora-types = { path = "../pandora-types" }
serde.workspace = true
serde_json.workspace = true
walkdir.workspace = true
